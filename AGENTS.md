# Repository Instructions

- Do not run `npm run build` automatically after every small UI or frontend-only change.
- For routine visual/layout edits, prefer a quick browser check when useful.
- Run a full build when dependencies, config, routing, shared utilities, or higher-risk code paths change, or when the user asks for it.
